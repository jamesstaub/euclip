import selectorsUtil from 'euclip/utils/selectors-util';
import { module, test } from 'qunit';

module('Unit | Utility | selectors-util', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = selectorsUtil();
    assert.ok(result);
  });
});
