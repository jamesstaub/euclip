<Page::Wrapper>
  <h1 class="tc">Sorry, something went wrong while trying to create a new project.</h1>
  <div class="mv3">
    {{#each this.model.errors as |err|}}
      <p>{{render-model-error err}}</p>
    {{/each}}
  </div>
</Page::Wrapper>
