<ListItem 
  @selected={{@selected}} 
  {{did-insert this.renderParts}} 
  ...attributes
>
  <ListItemLink {{on "click" (fn @onSelect)}}>
    {{#each parentDirs as |dir|}}
      {{dir}} /
    {{/each}}

    {{#if file}}
      {{filename-ui file}}
    {{/if}}
  </ListItemLink>

</ListItem>
