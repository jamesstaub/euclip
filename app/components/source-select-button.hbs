{{#if @onToggleFilePicker}}
  <ButtonPrimary
    class='ba w4 f7 bg-dark-gray truncate ma2 relative {{this.classNames}}'
    {{on 'click' @onToggleFilePicker}}
    @restyle={{true}}
    @label={{this.label}}
    ...attributes
  >
    {{if
      @filename
      @filename
      'Pick a Sound'
    }}
    {{#if @fileError}}
      <span class='absolute beacon right-1' title={{@fileError}}>⬤</span>
    {{/if}}
  </ButtonPrimary>
{{/if}}