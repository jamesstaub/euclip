<div class="flex pa3 justify-between flex-wrap"> 
  <SequencePagination 
    @sequence={{@sequence}} 
    @stepIndex={{@stepIndex}}
    @isCurrentTrack={{@isCurrentTrack}}
    as |sequencePaginated page currentPageStartIdx|
  >
    <div class="sequencer-width flex justify-start shrink-0 mr3">
      {{#each sequencePaginated as |step idx|}}
        <SequencerStep 
          {{on "click" (fn @onClickStep idx)}}
          @step={{step}} 
          @stepIndex={{seq-step-offset @stepIndex @sequence}} 
          @idx={{add idx currentPageStartIdx}}
        />
      {{/each}}
    </div>
  </SequencePagination>
</div>