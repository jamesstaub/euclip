<BasicDropdown as |dd|>
  <dd.Trigger ...attributes>
    <span class='caret'></span>
    {{#if (has-block)}}
        {{yield dd}}
    {{else}}
      {{svg-jar
        (or @icon 'fi-dots')
        class=(or @iconClass 'dark-fill')
        width='16px'
        height='40px'
      }}
    {{/if}}
  </dd.Trigger>

  <dd.Content class=''>
    <ul class="list pl0 ma0 bg-dark-gray ba bw2 b--light-gray shadow-3">
      {{#each @items as |item|}}
        <li>
          <button
            class='bg-transparent bn w-100 ph3 pv2 light-gray'
            type='button'
            title={{item.label}}
            {{on 'click' (fn this.onSelectItem item.select dd.actions.close)}}
          >
            {{item.label}}
          </button>
        </li>
      {{/each}}
    </ul>
  </dd.Content>
</BasicDropdown>